import socket

server = socket.socket()
server.bind(('0.0.0.0 то', 6677))
server.listen(10)
conn, addr = server.accept()

print('Connection established!\n'+str(addr))
while True:
	data = conn.recv(1024)
	if not data:
		break
	conn.send(data.upper())
conn.close()
